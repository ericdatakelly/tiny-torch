kind: WorkflowTemplate
metadata:
  name: visualize-template
spec:
  templates:
    - name: visualize-template
      container:
        image: ericdatakelly/nebari-workflow
        command: ["visualize.py"]
