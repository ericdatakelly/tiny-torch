kind: WorkflowTemplate
metadata:
  name: main-template
spec:
  templates:
    - name: main-template
      container:
        image: ignite
        command: ["main.py"]
      outputs:
        artifacts:
          - name: model
            path: output/model.joblib
