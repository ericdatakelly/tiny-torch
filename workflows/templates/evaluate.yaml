kind: WorkflowTemplate
metadata:
  name: evaluate-template
spec:
  templates:
    - name: evaluate-template
      container:
        image: ericdatakelly/nebari-workflow
        command: ["evaluate.py"]
